import styled from 'styled-components'

/** height & flex are to make container large enough to push footer to the bottom */
export const StyledMain = styled.main`
  min-height: 100%;
  flex-grow: 1;
`
